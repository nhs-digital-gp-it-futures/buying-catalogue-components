{% from 'components/details/macro.njk' import details %}

{% macro viewExpandableSection(params) %}
  <div data-test-id="{{ params.dataTestIdIdentifier }}" class="bc-c-expandable-section {{ params.classes }}">
    {{ details({
      "text": params.title,
      "HTML": params.innerComponent
    }) }}
  </div>
{% endmacro %}
