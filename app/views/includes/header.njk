{% from 'components/bc-header/macro.njk' import bcHeader %}
{% from 'components/login-logout/macro.njk' import loginLogout %}

{{ bcHeader({
    innerComponent: loginLogout({
      username: 'NHS Digital'
    }),
    classes: "nhsuk-header__container"
  })
}}
